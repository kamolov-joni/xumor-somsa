# Netlify build sozlamalari (frontend uchun)
[build]
  command = "npm run build"
  publish = "dist"

# React Router (SPA) — barcha yo'llar index.html'ga yo'naltiriladi
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
